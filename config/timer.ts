import { TimerConfig } from "types"

export const timerConfig: TimerConfig = {
  presets: [
        {
          GettingStarted: true,
          Solutions: true,
          Documentation: true,
        },
      ],
  mainNav: [
        {
          title: "Pricing",
          href: "/prices",
          disabled: true,
        },
      ],
}
